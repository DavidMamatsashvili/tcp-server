# Multi-threaded TCP Server and Client in C
A simple multi-threaded TCP chat server and client implementation using **POSIX threads (`pthread`)** in C.

## Compilation

### Compile the server
```bash
    gcc server.c -o server -pthread
```
### Compile the client
```bash
    gcc client.c -o client -pthread
```
